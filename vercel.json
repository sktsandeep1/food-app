{
  "projects": [
    {
      "src": "client/",
      "buildCommand": "npm run build",
      "outputDirectory": "client/dist"
    },
    {
      "src": "server/",
      "buildCommand": "npm run start"
    }
  ]
}
